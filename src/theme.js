// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#6599CC', // A custom orange color
        },
        secondary: {
            main: '#00355E', // A custom cyan color
        },
    },
});

export default theme;
